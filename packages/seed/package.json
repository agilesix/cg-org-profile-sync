{
  "name": "@cg-link/seed",
  "version": "0.0.0",
  "private": true,
  "description": "Seed organization profiles for the demo, deliberately out of step with each other",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cg-link/org-sync": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
