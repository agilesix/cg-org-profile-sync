# Link holds no credentials.
#
# Each system issues its own access token through its own OAuth flow, the
# browser keeps them for the session, and Link forwards them on the
# `x-source-tokens` header. There is nothing to put here — the file is kept so
# `cp .env.example .env` stays in the setup instructions alongside the portals,
# and so the next variable has somewhere to go.
